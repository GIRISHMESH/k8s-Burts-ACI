# Use official Nginx image
FROM nginx:alpine

# Copy your custom index.html (or app files) into Nginx web root
COPY ./index.html /usr/share/nginx/html/

# (Optional) Copy custom nginx.conf if you need reverse proxy config
# COPY ./nginx.conf /etc/nginx/nginx.conf

# Expose port 80
EXPOSE 80

# Start Nginx in foreground (default CMD from base image)
